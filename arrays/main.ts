//arrays
const superHeros: string[] = []

const power: string[] = []
//


type User = {
    name: string
    isActive: boolean
}

const allUsers: User[] = []

const mlModel: number[][] = [        //array of array
    [255,255,255],
    []
]

//pushing values to array
superHeros.push("Thor")
power.push("Hammer")

allUsers.push({name: "", isActive: true})
